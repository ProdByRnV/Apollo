# Build output ---------------------------------------------------------------
build/
build-*/
out/
cmake-build-*/
_deps/
CMakeUserPresets.json
compile_commands.json

# The standalone application, copied into the project root after every build so
# it can be launched without walking down the build tree
# (Source/Plugin/CMakeLists.txt). It is a build artefact that happens to live
# beside the documents, and is never committed.
/Apollo.exe
/Apollo
/Apollo.app/

# Frontend -------------------------------------------------------------------
node_modules/
WebUI/dist/
WebUI/build/
.vite/
*.tsbuildinfo

# IDE / editor ---------------------------------------------------------------
.vs/
.vscode/*
!.vscode/settings.json.example
.idea/
*.user
*.suo
*.sln.docstates
.cache/

# Local tooling --------------------------------------------------------------
.clangd-cache/
.claude/settings.local.json

# Operating system -----------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# Audio / profiling artefacts produced during DSP validation -----------------
# Regression renders that are meant to be tracked belong in Tests/ with an
# explicit path; anything dropped at the repository root is scratch.
/*.wav
/*.aiff
/*.flac
*.etl
*.nvvp
